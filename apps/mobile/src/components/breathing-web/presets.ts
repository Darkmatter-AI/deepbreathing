/**
 * Per-mode preset helpers — re-exported from the single shared source in
 * @resonance/domain.
 *
 * @see packages/domain/src/settings.ts
 */
export {
  type ModePreset,
  MAX_PRESET_DURATION_SEC,
  sanitizeModePresets,
  resolveModePreset,
} from '@resonance/domain';
