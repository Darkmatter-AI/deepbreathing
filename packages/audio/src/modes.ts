/**
 * Canonical mode identifiers — defined in @resonance/domain (single source),
 * re-exported here so existing `@resonance/audio` consumers keep working.
 */
export { ModeName } from '@resonance/domain';
