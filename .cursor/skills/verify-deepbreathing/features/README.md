# Deep Breathing Exercises — Feature Map

Top user-facing features on the web surface (Next.js 15). Use Playwright-based drivers from this skill to exercise each path against a locally launched instance.

- Homepage breathing session (`homepage-breathing.md`)
- Pattern page — Box breathing (`pattern-box.md`)
- Languages index (`languages-index.md`)
- Duration selection (`duration-selection.md`)

Harness: Playwright (Chromium). Real selectors use ARIA roles and accessible names as emitted by the app.
