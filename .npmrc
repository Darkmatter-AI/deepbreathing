# Expo SDK 56 supports pnpm isolated installs. Keeping each workspace's
# React/React Native tree isolated avoids the web app's React 18 hoist being
# picked up by apps/mobile (React 19.2.3 / RN 0.85).
node-linker=isolated
frozen-lockfile=false
